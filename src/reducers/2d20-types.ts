export interface InfinityState {
  showTokensModal: boolean;
  momentum: string;
  heat: string;
  assistanceDiceResults: Array<number>;
}

export interface ConanState {
  showTokensModal: boolean;
  momentum: string;
  doom: string;
  assistanceDiceResults: Array<number>;
}

export interface DuneState {
  showTokensModal: boolean;
  momentum: string;
  threat: string;
}
